export default {
    realWindowWidth : window.innerWidth, //화면 크기 저장
    realWindowHeight : window.innerHeight,

    myWindowWidth : (window.innerWidth/100)*98, //화면 크기 저장
    myWindowHeight : 180,

    adWindowWidth : window.innerWidth, //화면 크기 저장
    adWindowHeight : window.innerHeight/8
};